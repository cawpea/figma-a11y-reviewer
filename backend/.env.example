# Figma A11y Reviewer - Backend Environment Variables
# このファイルをコピーして .env を作成してください: cp .env.example .env

# Server Configuration
# PORT=3000
# 注意: PORTはCloud Functionsでは予約されているため、ローカル開発時のみ使用可能

# Debug Mode
# 開発環境では詳細なデバッグログ、スクリーンショット、プロンプトログが出力されます
DEBUG=false

# CORS Configuration
# 開発環境では "*" でOK、本番環境では適切なオリジンを設定してください
CORS_ORIGIN=*

# NODE_ENV について
# NODE_ENVは .env ファイルで設定する必要はありません
# npm scriptsで自動的に設定されます:
#   - npm run dev          → NODE_ENV=development
#   - npm run deploy  → NODE_ENV=production
