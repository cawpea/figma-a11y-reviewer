name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]
    # Optional: Only run on specific file changes
    # paths:
    #   - "src/**/*.ts"
    #   - "src/**/*.tsx"
    #   - "src/**/*.js"
    #   - "src/**/*.jsx"

jobs:
  claude-review:
    # Optional: Filter by PR author
    # if: |
    #   github.event.pull_request.user.login == 'external-contributor' ||
    #   github.event.pull_request.user.login == 'new-developer' ||
    #   github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'

    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            このプルリクエストをレビューし、以下の観点についてフィードバックを提供してください。
            - コード品質およびベストプラクティス
            - 潜在的なバグや問題点
            - パフォーマンスに関する懸念
            - セキュリティ上の懸念
            - テストカバレッジ
            - ドキュメント更新の必要性

            スタイルやコーディング規約については、リポジトリ内の `CLAUDE.md` をガイドとして参照してください。フィードバックは建設的かつ有益なものにしてください。
            レビュー内容は、Bashツールを使って `gh pr comment` コマンドでPRにコメントとして投稿してください。

            ドキュメント更新については、PRの変更点を分析し、`documentation-guidelines.md`の「4.1 更新が必要な変更」に該当するか判断してください。
            - 該当する場合は、影響を受けるドキュメントをリストアップし、具体的な更新提案を行ってください。
            - 該当しない場合は、その理由を簡潔に説明してください。

          # See https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md
          # or https://docs.claude.com/en/docs/claude-code/cli-reference for available options
          claude_args: '--allowed-tools "Bash(gh issue view:*),Bash(gh search:*),Bash(gh issue list:*),Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh pr list:*),Bash(gh pr review:*)"'
